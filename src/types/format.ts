export enum StatusType {
    "等待审核" = 0,
    "成功" = 1,
    "拒绝" = 9,
}

export enum StatusColor {
    "gray" = "#86909c",
    "green" = "#00b42a",
    "red" = "#f53f3f",
}

export enum ActiveType {
    "未開始" = 0,
    "進行中" = 1,
    "已結束" = 9,
}
